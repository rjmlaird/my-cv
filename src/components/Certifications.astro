---
import { certifications } from '@/data/certifications';
---

<section id="certifications" aria-labelledby="cert-heading">
  <p class="eyebrow">Credentials</p>
  <h2 id="cert-heading">Certifications</h2>
  <div class="cert-grid">
    {certifications.map((group) => (
      <div class="cert-group">
        <h3>{group.title}</h3>
        <ul>
          {group.items.map((item) => (
            <li>{item.href ? <a href={item.href} target="_blank" rel="noopener">{item.label}</a> : item.label}</li>
          ))}
        </ul>
        {group.subgroups?.map((sub) => (
          <>
            <p class="cert-subhead">{sub.subhead}</p>
            <ul>
              {sub.items.map((item) => (
                <li>{item.href ? <a href={item.href} target="_blank" rel="noopener">{item.label}</a> : item.label}</li>
              ))}
            </ul>
          </>
        ))}
      </div>
    ))}
  </div>
</section>
